<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  

<mapper namespace="com.smhrd.web.mapper.RegisterMapper우빈">

	<insert id="회원가입DB와연결메서드"  parameterType="com.smhrd.web.entity.Driver우빈">
	<!-- parameterType: DB로 보낼때, resultType: DB에서 받아서 다시 view로 보낼때-->
		INSERT INTO tb_driver 
        (driver_id, driver_pwd, driver_name, driver_region, driver_contact, driver_birthdate, driver_gender)
        VALUES 
        (#{driverId}, #{driverPwd}, #{driverName}, #{driverRegion}, #{driverContact}, #{driverBirthdate}, #{driverGender})
	</insert>
	
</mapper>