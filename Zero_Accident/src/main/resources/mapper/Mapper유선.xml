<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  

<mapper namespace="com.smhrd.web.mapper.Mapper유선">

    <select id="driverDetailLog" resultType="com.smhrd.web.dto.DriverDetail">
        SELECT
			tl.log_idx,
			tl.event_level,
			tl.event_color,
			tl.event_type,
			tl.event_video,
			tl.event_type_ko,
			tl.created_at,
			tl.reg_date,
			tl.display_addr,
			ta.admin_name as admin_name
        FROM
            tb_log tl  <!-- DRIVER_LOG 테이블에 DL 별칭 부여 -->
        INNER JOIN
            tb_admin ta on tl.admin_name = ta.name
        WHERE
            tl.log_idx = #{logIdx}
    </select>
</mapper>